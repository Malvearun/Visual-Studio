apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod
  labels:
    name: myapp
    type: front-end
spec:
  containers:
    - name: nginx-container
      image: nginx

# kubectl get pods
# kubectl create -f pod-definition.yaml
